apiVersion: v2
name: applications
description: App-of-apps root chart for the prod environment. Templates are added here as each component (backend, frontend, external-dns, etc.) is built.
type: application
version: 0.1.0
